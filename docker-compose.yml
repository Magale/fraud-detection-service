version: '3.8'
services:
  api:
    build: .
    ports:
      - "5000:80"
    depends_on:
      - postgres
  postgres:
    image: postgres
    environment:
      POSTGRES_DB: frauddb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
